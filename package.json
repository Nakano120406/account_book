{
  "name": "account_book",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "build": "node esbuild.js",
    "push": "clasp push",
    "open": "clasp open",
    "deploy": "npm run build && npm run push"
  },
  "devDependencies": {
    "esbuild": "^0.25.4",
    "esbuild-gas-plugin": "^0.9.0"
  }
}
